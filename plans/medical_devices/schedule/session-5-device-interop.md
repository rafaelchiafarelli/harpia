# Session 5 — Device Interoperability (moved)

This file's content moved to
[`thread-5-device-interop/`](thread-5-device-interop/README.md) — one
file per track (P/Q), each broken into small `Session <Track>.<n>` units
with its own receives/gives/touches contract. See that folder's
`README.md` for the thread-wide preconditions and execution order.

Kept as a stub, not deleted, since other docs may still reference this
exact filename until all cross-references are swept in one pass.
