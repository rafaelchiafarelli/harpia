# Session 2 — Transport & Access (moved)

This file's content moved to
[`thread-2-transport-and-access/`](thread-2-transport-and-access/README.md)
— one file per track (C/B), each broken into small `Session <Track>.<n>`
units with its own receives/gives/touches contract. See that folder's
`README.md` for the thread-wide preconditions and execution order.

Kept as a stub, not deleted, since `foundation.md` and other docs may
still reference this exact filename until Sessions 3–5 get the same
treatment and all cross-references are swept in one pass.
