# Session 4 — Platform Infra & Expansion (moved)

This file's content moved to
[`thread-4-platform-infra/`](thread-4-platform-infra/README.md) — one
file per track (J/M/N/L), each broken into small `Session <Track>.<n>`
units with its own receives/gives/touches contract, except Track L which
is still blocked on an open question (see that folder's `track-l-versioning.md`).
Track I is dropped entirely — already shipped via a different mechanism,
see `foundation.md`'s 2026-08-23 update note.

Kept as a stub, not deleted, since other docs may still reference this
exact filename until all cross-references are swept in one pass.
