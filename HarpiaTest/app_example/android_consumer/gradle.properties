# Required by the androidx.test.* dependencies app/build.gradle pulls in for
# the instrumented tests (J.25-J.27) -- AGP refuses to resolve an AndroidX
# dependency graph without this, even for a module with no other AndroidX use.
android.useAndroidX=true
