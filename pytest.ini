[pytest]
testpaths = UnitTests
# Don't write .pytest_cache into the (often mounted) working tree.
addopts = -p no:cacheprovider
markers =
    fuzz: bounded, deterministic parser fuzz runs (static-fuzz-ci epic); runs by default, `-m "not fuzz"` skips
