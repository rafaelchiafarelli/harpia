// Generated by harpia (session J.6, thread-1-java-target). Do not edit.
// Deferred columns (embed/singular-FK-to-table, not yet supported for the
// Java target's CRUDL DAO -- see JavaDatabase/CLAUDE.md): {deferred_note}
package com.harpia.generated.db;

import com.harpia.generated.{name};
import com.harpia.runtime.db.JdbcBind;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.List;

public final class {name}_dao {{
    private final Connection conn;

    public {name}_dao(Connection conn) {{
        this.conn = conn;
    }}

    public static final String CREATE_TABLE_SQL = "{create_table_sql}";
    public static final String DROP_TABLE_SQL = "{drop_table_sql}";

    public boolean createTable() throws SQLException {{
        try (PreparedStatement ps = conn.prepareStatement(CREATE_TABLE_SQL)) {{
            ps.execute();
        }}
        return true;
    }}

    public boolean dropTable() throws SQLException {{
        try (PreparedStatement ps = conn.prepareStatement(DROP_TABLE_SQL)) {{
            ps.execute();
        }}
        return true;
    }}

    public boolean create({name} msg) throws SQLException {{
        try (PreparedStatement ps = conn.prepareStatement("{insert_sql}")) {{
{bind_lines}
            return ps.executeUpdate() > 0;
        }}
    }}

    public boolean read({pk_java_type} id, {name}.Builder builder) throws SQLException {{
        try (PreparedStatement ps = conn.prepareStatement("{select_by_pk_sql}")) {{
            ps.set{pk_setter}(1, id);
            try (ResultSet rs = ps.executeQuery()) {{
                if (!rs.next()) {{
                    return false;
                }}
{extract_lines}
                return true;
            }}
        }}
    }}

    public boolean update({name} msg) throws SQLException {{
        try (PreparedStatement ps = conn.prepareStatement("{update_sql}")) {{
{update_bind_lines}
            JdbcBind.bind(ps, {update_pk_index}, msg, "{pk_field}");
            return ps.executeUpdate() > 0;
        }}
    }}

    public boolean remove({pk_java_type} id) throws SQLException {{
        try (PreparedStatement ps = conn.prepareStatement("{delete_sql}")) {{
            ps.set{pk_setter}(1, id);
            return ps.executeUpdate() > 0;
        }}
    }}

    public boolean list(List<{name}> out) throws SQLException {{
        try (PreparedStatement ps = conn.prepareStatement("{select_all_sql}");
             ResultSet rs = ps.executeQuery()) {{
            while (rs.next()) {{
                {name}.Builder builder = {name}.newBuilder();
{extract_lines}
                out.add(builder.build());
            }}
            return true;
        }}
    }}
}}
