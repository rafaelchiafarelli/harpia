## Documentation index (`phi`, and tying it together)

- **Depends on:** task 1-task 7 (indexes all of them).
- **Deliverable:** new `HarpiaTest/app_example/README.md` indexing every example
  (`consumer`, `android_consumer`, and task 2-task 7's new ones), plus a `phi`
  section — documentation only, no compiled program — pointing at
  `patient_vitals` (already in the fixture) and `UnitTests/run_phi_check.py`'s
  existing reflection output, explicit that no encryption/redaction/audit
  exists yet. Root `README.md` gets one bullet update once this lands.
- **Tests:** none — documentation session.
