# Generated by harpia Stage 14 (unit tests). Do not edit.
#
# Built only when the top-level CMake is configured with
# -DHARPIA_BUILD_TESTS=ON; run with `ctest` from the build dir.
find_package(Threads)

# The DB layer is emitted against SOCI (soci_core + the sqlite3
# backend). apt's soci-dev has no CMake CONFIG package, only bare
# .so names findable by the linker's default search path; vcpkg's
# soci port exports SOCI::SOCI (all installed backend components).
if(WIN32)
    # SOCI::SQLite3's own link interface references the
    # SQLite3::SQLite3 imported target without importing it itself.
    # vcpkg's sqlite3 port has no CONFIG package under that name
    # (only "unofficial-sqlite3", target unofficial::sqlite3::sqlite3)
    # and CMake's builtin MODULE-mode FindSQLite3 doesn't reliably win
    # the search under the vcpkg toolchain, so alias it in by hand --
    # a known vcpkg soci-port quirk.
    find_package(unofficial-sqlite3 CONFIG REQUIRED)
    if(NOT TARGET SQLite3::SQLite3)
        add_library(SQLite3::SQLite3 ALIAS unofficial::sqlite3::sqlite3)
    endif()
    find_package(SOCI CONFIG REQUIRED)
    set(HARPIA_SOCI_LIBS SOCI::SOCI)
else()
    set(HARPIA_SOCI_LIBS soci_core soci_sqlite3)
endif()

if(WIN32)
    # generated/cpp/protofiles/*.pb.h was baked by the pipeline's
    # Docker protoc (apt's, an old version) and can't compile
    # against whatever much newer protobuf vcpkg installs (Google
    # has removed/moved internal headers like map_field_inl.h
    # across major versions). proto/CMakeLists.txt already
    # regenerates matching .pb.h from the raw .proto via vcpkg's
    # own protoc into ${CMAKE_BINARY_DIR}/proto/protofiles -- list
    # that dir FIRST (same fix as Assets/server_template) so the
    # adapter headers' quoted #include "protofiles/...pb.h"
    # resolves there instead of the stale baked copy.
    set(HARPIA_TEST_PROTO_INCLUDE_DIR ${CMAKE_BINARY_DIR}/proto)
else()
    set(HARPIA_TEST_PROTO_INCLUDE_DIR)
endif()

# Vendored tinyxml2 for the SOAP credential (access-rights) tests.
add_library(harpia_tinyxml2 STATIC
    ${CMAKE_SOURCE_DIR}/third_party/tinyxml2/tinyxml2.cpp)
target_include_directories(harpia_tinyxml2 PUBLIC
    ${CMAKE_SOURCE_DIR}/third_party/tinyxml2)

add_executable(data_test data_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(data_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(data_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(data_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(data_test PRIVATE ws2_32)
endif()
add_test(NAME data_test COMMAND data_test)

add_executable(users_test users_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(users_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(users_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(users_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(users_test PRIVATE ws2_32)
endif()
add_test(NAME users_test COMMAND users_test)

add_executable(vip_users_test vip_users_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(vip_users_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(vip_users_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(vip_users_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(vip_users_test PRIVATE ws2_32)
endif()
add_test(NAME vip_users_test COMMAND vip_users_test)

add_executable(top_users_test top_users_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(top_users_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(top_users_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(top_users_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(top_users_test PRIVATE ws2_32)
endif()
add_test(NAME top_users_test COMMAND top_users_test)

add_executable(shipment_test shipment_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(shipment_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(shipment_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(shipment_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(shipment_test PRIVATE ws2_32)
endif()
add_test(NAME shipment_test COMMAND shipment_test)

add_executable(journey_test journey_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(journey_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(journey_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(journey_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(journey_test PRIVATE ws2_32)
endif()
add_test(NAME journey_test COMMAND journey_test)

add_executable(crew_test crew_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(crew_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(crew_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(crew_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(crew_test PRIVATE ws2_32)
endif()
add_test(NAME crew_test COMMAND crew_test)

add_executable(outpost_test outpost_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(outpost_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(outpost_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(outpost_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(outpost_test PRIVATE ws2_32)
endif()
add_test(NAME outpost_test COMMAND outpost_test)

add_executable(beacon_log_test beacon_log_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(beacon_log_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(beacon_log_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(beacon_log_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(beacon_log_test PRIVATE ws2_32)
endif()
add_test(NAME beacon_log_test COMMAND beacon_log_test)

add_executable(patient_vitals_test patient_vitals_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(patient_vitals_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(patient_vitals_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(patient_vitals_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(patient_vitals_test PRIVATE ws2_32)
endif()
add_test(NAME patient_vitals_test COMMAND patient_vitals_test)

add_executable(alarm_event_test alarm_event_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(alarm_event_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(alarm_event_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(alarm_event_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(alarm_event_test PRIVATE ws2_32)
endif()
add_test(NAME alarm_event_test COMMAND alarm_event_test)

add_executable(telemetry_test telemetry_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(telemetry_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(telemetry_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(telemetry_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(telemetry_test PRIVATE ws2_32)
endif()
add_test(NAME telemetry_test COMMAND telemetry_test)

add_executable(app_test app_3ac5d8b36fc7dcfb70888145147ddfb7_test.cpp)
target_include_directories(app_test PRIVATE
    ${HARPIA_TEST_PROTO_INCLUDE_DIR}
    ${CMAKE_SOURCE_DIR}/generated/cpp
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/crow
    ${CMAKE_SOURCE_DIR}/third_party/asio)
target_link_libraries(app_test PRIVATE protofiles harpia_tinyxml2 ${HARPIA_SOCI_LIBS} Threads::Threads ${CMAKE_DL_LIBS})
if(WIN32)
    # Crow/asio pull in <windows.h>; without these its full
    # umbrella pollutes unrelated identifiers elsewhere in the
    # same translation unit (e.g. GetMessage, min/max).
    target_compile_definitions(app_test PRIVATE WIN32_LEAN_AND_MEAN NOMINMAX)
    # harpia_test_client.h (REST/SOAP HTTP round-trip client)
    # is Winsock2 on this platform; its own #pragma comment
    # covers MSVC, but link explicitly too for other Windows
    # toolchains (e.g. MinGW, which ignores pragma comment).
    target_link_libraries(app_test PRIVATE ws2_32)
endif()
add_test(NAME app_test COMMAND app_test)
