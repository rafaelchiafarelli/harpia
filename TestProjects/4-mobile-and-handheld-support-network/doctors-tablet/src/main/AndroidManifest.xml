<?xml version="1.0" encoding="utf-8"?>
<!--
  Minimal on purpose. This module exists to show how a Mobile-device project
  consumes harpia's Java-target output, not to be a shipping app. No launcher
  Activity - the consumed surface (message classes, JSON, gRPC/ZMQ clients) is
  plain Java exercised from DeviceApp/HumanMock main().
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="harpia_doctorstablet"
        android:allowBackup="false" />

</manifest>
