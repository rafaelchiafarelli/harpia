// Generated by harpia Stage 10 (YAML adapter). Do not edit.
#ifndef {guard}
#define {guard}

#include <string>

#include "yaml/harpia_yaml.h"
#include "{pb_header}"

// YAML for {name} is provided by the generic reflection-based runtime:
//   std::string yaml = harpia::yaml::to_yaml(msg);   // {name} -> YAML
//   harpia::yaml::from_yaml(yaml, &msg);             // YAML -> {name}
//
// This wrapper exists only to pull the runtime in alongside the message's
// protobuf header, matching the JSON/XML adapters' per-message shape.

#endif  // {guard}
